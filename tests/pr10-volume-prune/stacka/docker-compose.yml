version: '2.4'
services:
  alpine2:
    image: nginx:latest
    working_dir: /data
    volumes:
      - test-volume:/data

volumes:
  test-volume:
    driver: antoniospatera/glusterfs:next
    driver_opts:
      servers: "172.28.0.10,172.28.0.11"
      volname: gv0
      subdir: stacka-foo
